/** Shared class strings reused across small UI pieces so they can't drift. */

/** Square 36px icon button — navbar menu toggle + theme switch. */
export const iconButtonClass =
	'flex size-9 items-center justify-center rounded-lg text-foreground transition-colors hover:bg-default hover:text-accent';
